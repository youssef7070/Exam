1. update-1

- in Phone filed in Create Account add country flags as dropdwon  ( Done )
- Make auth routes as file in auth feature and import it in app routes
( Done)
- Make one reusable button
- Make a auth layout and use router outlet and dont repeat app-fixed-side (Done)
- Make password match validator as utility function, since we will use same validator in Register, Reset Password, Change Password (Done)
- Create reusable input error message component (Done)
- Make reusable Error banner component (Done)